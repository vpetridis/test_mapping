import React from "react";
import imageData from "./ImageData";

export default function App() {
    // const slicedItems = imageData
    //     .map((image) => <Item key={image.id} imageData={image} />)
    //     .slice(2, 50);

    return (
        <div >
        </div>
    );
}
